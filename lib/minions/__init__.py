"""minions — personal one-shot coding agent orchestrator (goose-powered).

Inspired by Stripe's "Minions" blog series: a blueprint state machine that
interleaves deterministic nodes (git ops, linters, tests) with agent nodes
(goose runs) to produce a branch / PR with no human interaction.
"""
__version__ = "0.1.0"
